services:
  meteoai:
    build: .
    container_name: meteoai-app
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434/v1
    extra_hosts:
      - "host.docker.internal:host-gateway"